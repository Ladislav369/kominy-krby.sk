# Copy to .env and fill in. Never commit .env (already in .gitignore).
FTP_HOST=ftp.kominy-krby.sk
FTP_USER=
FTP_PASS=
# Remote target dir on the server (Websupport: usually /web/ or /www/).
FTP_REMOTE_DIR=/web/
# Local dir to upload (defaults to repo root if not set).
LOCAL_DIR=.
